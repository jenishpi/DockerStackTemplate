{    
    email {env.DUCKDNS_EMAIL}
}

*.{env.DUCKDNS_SUBDOMAINNAME}.duckdns.org {
    tls {
        dns duckdns {env.DUCKDNS_TOKEN}
    }    

    @vaultwarden host vaultwarden.{env.DUCKDNS_SUBDOMAINNAME}.duckdns.org
    handle @vaultwarden {        
        reverse_proxy {env.DUCKDNS_IP}:8089
    }

    @postgresweb host postgesweb.{env.DUCKDNS_SUBDOMAINNAME}.duckdns.org
    handle @postgresweb {        
        reverse_proxy {env.DUCKDNS_IP}:8087
    }

    @postgres host postgres.{env.DUCKDNS_SUBDOMAINNAME}.duckdns.org
    handle @postgres {        
        reverse_proxy {env.DUCKDNS_IP}:5432
    }
        
    handle {
        respond "Not Found" 404
    }
}
